== Densidades independentes de pixels

[Assista ao vídeo](https://www.youtube.com/watch?v=zhszwkcay2A)

=== EXEMPLOS PRATICOS (IMC - sem MVC)

- activity_main.xml
[source,xml]
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Informe seus dados:"
        android:layout_marginBottom="16dp" />
    <EditText
        android:id="@+id/editTextNome"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nome"
        android:inputType="textPersonName" />
    <EditText
        android:id="@+id/editTextIdade"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Idade"
        android:inputType="number" />
    <EditText
        android:id="@+id/editTextAltura"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Altura (em metros)"
        android:inputType="numberDecimal" />
    <EditText
        android:id="@+id/editTextPeso"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Peso (em kg)"
        android:inputType="numberDecimal" />
    <Button
        android:id="@+id/buttonCalcular"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Calcular IMC"
        android:layout_marginTop="16dp" />
    <TextView
        android:id="@+id/textViewResultado"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        android:layout_marginTop="16dp" />
</LinearLayout>

- MainActivity.java
[source,java]
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.TextView;
import androidx.appcompat.app.AppCompatActivity;
public class MainActivity extends AppCompatActivity {
    private EditText editTextNome, editTextIdade,
            editTextAltura, editTextPeso;
    private TextView textViewResultado;
    private Button buttonCalcular;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        editTextNome = findViewById(R.id.editTextNome);
        editTextIdade = findViewById(R.id.editTextIdade);
        editTextAltura = findViewById(R.id.editTextAltura);
        editTextPeso = findViewById(R.id.editTextPeso);
        buttonCalcular = findViewById(R.id.buttonCalcular);
        textViewResultado = findViewById(R.id.textViewResultado);
        buttonCalcular.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                String nome = editTextNome.getText().toString();
                int idade = Integer.parseInt(editTextIdade.getText().toString());
                double altura = Double.parseDouble(editTextAltura.getText().toString());
                double peso = Double.parseDouble(editTextPeso.getText().toString());
                Pessoa pessoa = new Pessoa(nome, idade, altura, peso);
                double imc = pessoa.calcularIMC();
                textViewResultado.setText("Nome: " + pessoa.getNome() + "\nIMC: " + String.format("%.2f", imc));
            }
        });
    }
}

- Pessoa.java
[source,java]
public class Pessoa {
    private String nome;
    private int idade;
    private double altura;
    private double peso;
    public Pessoa(String nome, int idade, double altura, double peso) {
        this.nome = nome;
        this.idade = idade;
        this.altura = altura;
        this.peso = peso;
    }
    public double calcularIMC() {
        return peso / (altura * altura);
    }
    public String getNome() {
        return nome;
    }
}

=== EXEMPLOS PRATICOS (IMC - com MVC)

- activity_main.xml
[source,xml]
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Informe seus dados:"
        android:layout_marginBottom="16dp" />
    <EditText
        android:id="@+id/editTextNome"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nome"
        android:inputType="textPersonName" />
    <EditText
        android:id="@+id/editTextIdade"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Idade"
        android:inputType="number" />
    <EditText
        android:id="@+id/editTextAltura"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Altura (em metros)"
        android:inputType="numberDecimal" />
    <EditText
        android:id="@+id/editTextPeso"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Peso (em kg)"
        android:inputType="numberDecimal" />
    <Button
        android:id="@+id/buttonCalcular"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Calcular IMC"
        android:layout_marginTop="16dp" />
    <TextView
        android:id="@+id/textViewResultado"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        android:layout_marginTop="16dp" />
</LinearLayout>

- MainActivity.java
[source,java]
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.TextView;
import androidx.appcompat.app.AppCompatActivity;
public class MainActivity extends AppCompatActivity {
    private EditText editTextNome, editTextIdade, editTextAltura, editTextPeso;
    private TextView textViewResultado;
    private Button buttonCalcular;
    private Controle controle;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        editTextNome    = findViewById(R.id.editTextNome);
        editTextIdade   = findViewById(R.id.editTextIdade);
        editTextAltura  = findViewById(R.id.editTextAltura);
        editTextPeso    = findViewById(R.id.editTextPeso);
        buttonCalcular  = findViewById(R.id.buttonCalcular);
        textViewResultado = findViewById(R.id.textViewResultado);
        controle = new Controle();
        buttonCalcular.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                String nome   = editTextNome.getText().toString();
                String idade  = editTextIdade.getText().toString();
                String altura = editTextAltura.getText().toString();
                String peso   = editTextPeso.getText().toString();
                String resultado = controle.calcularIMC(nome, idade, altura, peso);
                textViewResultado.setText(resultado);
            }
        });
    }//
}//class

- Pessoa.java
[source, java]
public class Pessoa {
    private String nome;
    private int idade;
    private double altura;
    private double peso;
    public Pessoa(String nome, int idade, double altura, double peso) {
        this.nome = nome;
        this.idade = idade;
        this.altura = altura;
        this.peso = peso;
    }
    public double calcularIMC() {
        return peso / (altura * altura);
    }
    public String getNome() {
        return nome;
    }
    public int getIdade() {
        return idade;
    }
    public double getAltura() {
        return altura;
    }
    public double getPeso() {
        return peso;
    }
}

- Controle.java
[source,java]
public class Controle {
    public String calcularIMC(String nomeStr, String idadeStr, String alturaStr, String pesoStr) {
            int idade = Integer.parseInt(idadeStr.trim());
            double altura = Double.parseDouble(alturaStr.trim());
            double peso = Double.parseDouble(pesoStr.trim());
            String nome = nomeStr.trim();
            Pessoa pessoa = new Pessoa(nome, idade, altura, peso);
            double imc = pessoa.calcularIMC();
            return "Nome: " + pessoa.getNome() + "\nIMC: " + String.format("%.2f", imc);
    }
}

- MainActivity.kt
[source,kt]
import android.os.Bundle
import android.widget.Button
import android.widget.EditText
import android.widget.TextView
import androidx.appcompat.app.AppCompatActivity
class MainActivity : AppCompatActivity() {
    private lateinit var editTextNome: EditText
    private lateinit var editTextIdade: EditText
    private lateinit var editTextAltura: EditText
    private lateinit var editTextPeso: EditText
    private lateinit var textViewResultado: TextView
    private lateinit var buttonCalcular: Button
    private lateinit var controle: Controle
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
        editTextNome = findViewById(R.id.editTextNome)
        editTextIdade = findViewById(R.id.editTextIdade)
        editTextAltura = findViewById(R.id.editTextAltura)
        editTextPeso = findViewById(R.id.editTextPeso)
        buttonCalcular = findViewById(R.id.buttonCalcular)
        textViewResultado = findViewById(R.id.textViewResultado)
        controle = Controle()
        buttonCalcular.setOnClickListener {
            val nome = editTextNome.text.toString()
            val idade = editTextIdade.text.toString()
            val altura = editTextAltura.text.toString()
            val peso = editTextPeso.text.toString()
            val resultado = controle.calcularIMC(nome, idade, altura, peso)
            textViewResultado.text = resultado
        }
    }
}

- Pessoa.kt
[source,kt]
data class Pessoa(
    val nome: String,
    val idade: Int,
    val altura: Double,
    val peso: Double
) {
    fun calcularIMC(): Double {
        return peso / (altura * altura)
    }
}

- Controle.kt
[source,kt]
import java.util.Locale
class Controle {
    fun calcularIMC(nomeStr: String, idadeStr: String, alturaStr: String, pesoStr: String): String {
        val idade = idadeStr.trim().toInt()
        val altura = alturaStr.trim().toDouble()
        val peso = pesoStr.trim().toDouble()
        val nome = nomeStr.trim()
        val pessoa = Pessoa(nome, idade, altura, peso)
        val imc = pessoa.calcularIMC()
        return "Nome: ${pessoa.nome}\nIMC: %.2f".format(Locale.US, imc)
    }
}

=== EXEMPLOS PRATICOS (Calculadora - sem MVC)

- activity_main.xml
[source,xml]
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center">
    <EditText
        android:id="@+id/editTextNumero1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Número 1"
        android:inputType="number" />
    <EditText
        android:id="@+id/editTextNumero2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Número 2"
        android:inputType="number" />
    <Button
        android:id="@+id/buttonSomar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Somar" />
    <Button
        android:id="@+id/buttonSubtrair"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Subtrair" />
    <TextView
        android:id="@+id/textViewResultado"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Resultado: "
        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        android:layout_marginTop="20dp" />
</LinearLayout>

- MainActivity.java
[source,java]
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.TextView;
import android.widget.Toast;
import androidx.appcompat.app.AppCompatActivity;
public class MainActivity extends AppCompatActivity {
    private EditText editTextNumero1, editTextNumero2;
    private Button buttonSomar, buttonSubtrair;
    private TextView textViewResultado;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        editTextNumero1 = findViewById(R.id.editTextNumero1);
        editTextNumero2 = findViewById(R.id.editTextNumero2);
        buttonSomar = findViewById(R.id.buttonSomar);
        buttonSubtrair = findViewById(R.id.buttonSubtrair);
        textViewResultado = findViewById(R.id.textViewResultado);
        buttonSomar.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                String numero1 = editTextNumero1.getText().toString();
                String numero2 = editTextNumero2.getText().toString();
                int num1 = Integer.parseInt(numero1);
                int num2 = Integer.parseInt(numero2);
                int resultado = Calculadora.somar(num1, num2);
                textViewResultado.setText("Resultado da soma: " + resultado);
            }
        });
        buttonSubtrair.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                String numero1Str = editTextNumero1.getText().toString();
                String numero2Str = editTextNumero2.getText().toString();
                int num1 = Integer.parseInt(numero1Str);
                int num2 = Integer.parseInt(numero2Str);
                int resultado = Calculadora.subtrair(num1, num2);
                textViewResultado.setText("Resultado da subtração: " + resultado);
            }
        });
    }
}

- Calculadora.java
[source,java]
public class Calculadora {
    public static int somar(int num1, int num2) {
        return num1 + num2;
    }
    public static int subtrair(int num1, int num2) {
        return num1 - num2;
    }
}

=== EXEMPLOS PRATICOS (Calculadora - com MVC)

- activity_main.xml
[source,xml]
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center">
    <EditText
        android:id="@+id/editTextNumero1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Número 1"
        android:inputType="number" />
    <EditText
        android:id="@+id/editTextNumero2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Número 2"
        android:inputType="number" />
    <Button
        android:id="@+id/buttonSomar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Somar" />
    <Button
        android:id="@+id/buttonSubtrair"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Subtrair" />
    <TextView
        android:id="@+id/textViewResultado"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Resultado: "
        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        android:layout_marginTop="20dp" />
</LinearLayout>

- MainActivity.java
[source,java]
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.TextView;
import android.widget.Toast;
import androidx.appcompat.app.AppCompatActivity;
public class MainActivity extends AppCompatActivity {
    private EditText editTextNumero1, editTextNumero2;
    private Button buttonSomar, buttonSubtrair;
    private TextView textViewResultado;
    private CalculadoraController controller;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        editTextNumero1   = findViewById(R.id.editTextNumero1);
        editTextNumero2   = findViewById(R.id.editTextNumero2);
        buttonSomar       = findViewById(R.id.buttonSomar);
        buttonSubtrair    = findViewById(R.id.buttonSubtrair);
        textViewResultado = findViewById(R.id.textViewResultado);
        controller = new CalculadoraController();
        buttonSomar.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                String num1 = editTextNumero1.getText().toString();
                String num2 = editTextNumero2.getText().toString();
                String resultado = controller.realizarOperacao(num1, num2, Operacao.SOMAR);
                textViewResultado.setText(resultado);
            }
        });
        buttonSubtrair.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                String num1 = editTextNumero1.getText().toString();
                String num2 = editTextNumero2.getText().toString();
                String resultado = controller.realizarOperacao(num1, num2, Operacao.SUBTRAIR);
                textViewResultado.setText(resultado);
            }
            });
        }
    }

- CalculadoraController.java
[source,java]
public class CalculadoraController {
    public String realizarOperacao(
            String num1Str,
            String num2Str,
            Operacao operacao) {
            int num1 = Integer.parseInt(num1Str.trim());
            int num2 = Integer.parseInt(num2Str.trim());
            int resultado;
            switch (operacao) {
                case SOMAR:
                    resultado = Calculadora.somar(num1, num2);
                    return "Resultado da soma: " + resultado;
                case SUBTRAIR:
                    resultado = Calculadora.subtrair(num1, num2);
                    return "Resultado da subtração: " + resultado;
                default:
                    throw new IllegalArgumentException("erro!!");
            }
        }
}

- Calculadora.java
[source,java]
public class Calculadora {
    public static int somar(int num1, int num2) {
        return num1 + num2;
    }
    public static int subtrair(int num1, int num2) {
        return num1 - num2;
    }
}

- Operacao.java
[source,java]
public enum Operacao {
    SOMAR,
    SUBTRAIR
}

- MainActivity.kt
[source,kt]
import android.os.Bundle
import android.widget.Button
import android.widget.EditText
import android.widget.TextView
import androidx.appcompat.app.AppCompatActivity
class MainActivity : AppCompatActivity() {
    private lateinit var editTextNumero1: EditText
    private lateinit var editTextNumero2: EditText
    private lateinit var buttonSomar: Button
    private lateinit var buttonSubtrair: Button
    private lateinit var textViewResultado: TextView
    private lateinit var controller: CalculadoraController
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
        editTextNumero1 = findViewById(R.id.editTextNumero1)
        editTextNumero2 = findViewById(R.id.editTextNumero2)
        buttonSomar = findViewById(R.id.buttonSomar)
        buttonSubtrair = findViewById(R.id.buttonSubtrair)
        textViewResultado = findViewById(R.id.textViewResultado)
        controller = CalculadoraController()
        buttonSomar.setOnClickListener {
            processarOperacao(Operacao.SOMAR)
        }
        buttonSubtrair.setOnClickListener {
            processarOperacao(Operacao.SUBTRAIR)
        }
    }
    private fun processarOperacao(operacao: Operacao) {
        val num1 = editTextNumero1.text.toString()
        val num2 = editTextNumero2.text.toString()
        val resultado = controller.realizarOperacao(num1, num2, operacao)
        textViewResultado.text = resultado
    }
}

- CalculadoraController.kt
[source,kt]
class CalculadoraController {
    fun realizarOperacao(num1Str: String, num2Str: String, operacao: Operacao): String {
        val num1 = num1Str.trim().toInt()
        val num2 = num2Str.trim().toInt()
        return when (operacao) {
            Operacao.SOMAR -> {
                val resultado = Calculadora.somar(num1, num2)
                "Resultado da soma: $resultado"
            }
            Operacao.SUBTRAIR -> {
                val resultado = Calculadora.subtrair(num1, num2)
                "Resultado da subtração: $resultado"
            }
            else -> throw IllegalArgumentException("erro!!")
        }
    }
}

- Calculadora.kt
[source,kt]
object Calculadora {
    fun somar(num1: Int, num2: Int): Int {
        return num1 + num2
    }
    fun subtrair(num1: Int, num2: Int): Int {
        return num1 - num2
    }
}

- Operacao.kt
[source,kt]
enum class Operacao {
    SOMAR,
    SUBTRAIR
}

=== EXEMPLOS PRATICOS (App Lanchonete)

- colors.xml
[source,xml]
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <color name="black">#FF000000</color>
    <color name="white">#FFFFFFFF</color>
    <color name="blue">#0D47A1</color>
    <color name="green">#C8E6C9</color>
</resources>

- string.xml
[source,xml]
<resources>
    <string name="app_name">Lanchonete_MVC</string>
    <string name="bolo_de_fuba">Bolo de Fubá</string>
    <string name="cafe">Café</string>
    <string name="exemplo_usando_tablelayout">Exemplo usando TableLayout</string>
    <string name="bolo">Bolo</string>
    <string name="quantas_unidades_de_cafe">Quantas unidades?</string>
    <string name="add">ADD</string>
    <string name="r_0_00">R$: 0,00</string>
</resources>

- activity_main.xml
[source,xml]
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">
    <TextView
        android:id="@+id/textViewTotal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:gravity="center"
        android:paddingBottom="24dp"
        android:textColor="@color/black"
        android:textSize="50sp"
        android:text="0"
        android:textStyle="bold" />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="center">
        <LinearLayout
            android:id="@+id/layout_cafe"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="16dp">
            <ImageView
                android:id="@+id/imageViewCafe"
                android:layout_width="140dp"
                android:layout_height="140dp"
                android:src="@drawable/cup"
                android:scaleType="fitCenter"
                android:contentDescription="@string/e" />
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/cafe"
                android:textSize="18sp"
                android:textColor="@color/black"
                android:layout_marginTop="12dp" />
        </LinearLayout>
        <LinearLayout
            android:id="@+id/layout_bolo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="16dp">
            <ImageView
                android:id="@+id/imageViewBolo"
                android:layout_width="140dp"
                android:layout_height="140dp"
                android:src="@drawable/cake"
                android:scaleType="fitCenter"
                android:contentDescription="@string/adicionar_bolo" />
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/bolo_de_fuba"
                android:textSize="18sp"
                android:textColor="@color/black"
                android:layout_marginTop="12dp" />
        </LinearLayout>
    </LinearLayout>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/exemplo_usando_tablelayout"
        android:textSize="16sp"
        android:gravity="center"
        android:textStyle="italic"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="8dp" />
    <TableLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:stretchColumns="*">
        <TableRow>
            <ImageView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:src="@drawable/cup"
                android:padding="8dp"
                android:scaleType="fitCenter" />
            <ImageView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:src="@drawable/cake"
                android:padding="8dp"
                android:scaleType="fitCenter" />
        </TableRow>
        <TableRow>
            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="#BBDEFB"
                android:gravity="center"
                android:padding="12dp"
                android:text="@string/cafe"
                android:textColor="@color/blue"
                android:textSize="16sp" />
            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="@color/green"
                android:gravity="center"
                android:padding="12dp"
                android:text="@string/bolo"
                android:textSize="16sp" />
        </TableRow>
    </TableLayout>
</LinearLayout>

- activity_cafe.xml
[source,xml]
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center">
    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/cup"
        android:scaleType="fitCenter"
        android:layout_marginBottom="24dp"
        android:contentDescription="@string/cafe" />
    <EditText
        android:id="@+id/editTextCafe"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/quantas_unidades_de_cafe"
        android:inputType="number"
        android:textSize="18sp"
        android:padding="12dp"
        android:layout_marginBottom="24dp" />
    <Button
        android:id="@+id/buttonCafe"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/add"
        android:textSize="18sp"
        android:paddingHorizontal="32dp"
        android:paddingVertical="12dp"
        />
    <TextView
        android:id="@+id/textViewCafe"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textColor="@color/black"
        android:textSize="28sp"
        android:text="0"
        android:textStyle="bold" />
</LinearLayout>

- activity_bolo.xml
[source,xml]
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center">
    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/cake"
        android:scaleType="fitCenter"
        android:layout_marginBottom="24dp"
        android:contentDescription="@string/bolo_de_fuba" />
    <EditText
        android:id="@+id/et_quantidade_bolo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/quantas_unidades_de_cafe"
        android:inputType="number"
        android:textSize="18sp"
        android:padding="12dp"
        android:layout_marginBottom="24dp" />
    <Button
        android:id="@+id/btn_adicionar_bolo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/add"
        android:textSize="18sp"
        android:paddingHorizontal="32dp"
        android:paddingVertical="12dp"
        android:backgroundTint="@color/rose"
      />
</LinearLayout>

- MainActivity.java
[source,java]
import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.ImageView;
import android.widget.TextView;
import androidx.activity.EdgeToEdge;
import androidx.appcompat.app.AppCompatActivity;
import androidx.core.graphics.Insets;
import androidx.core.view.ViewCompat;
import androidx.core.view.WindowInsetsCompat;
public class MainActivity extends AppCompatActivity {
   private ImageView imageViewCafe, imageViewBolo;
   private TextView textViewTotal;
   private Double resultado = 0.0;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        EdgeToEdge.enable(this);
        setContentView(R.layout.activity_main);
        imageViewCafe = findViewById(R.id.imageViewCafe);
        imageViewBolo = findViewById(R.id.imageViewBolo);
        textViewTotal = findViewById(R.id.textViewTotal);
        imageViewBolo.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Intent intent = new Intent(MainActivity.this, BoloActivity.class);
                intent.putExtra("total", resultado); //envia o valor atual na MainActivity
                startActivity(intent);
            }
        });
        imageViewCafe.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Intent intent = new Intent(MainActivity.this, CafeActivity.class);
                intent.putExtra("total", resultado); //envia o valor atual na MainActivity
                startActivity(intent);
            }
        });
    }//onCreate
    @Override
    protected void onResume() {
        super.onResume();
        atualizarTela();
    }
    private void atualizarTela(){
        Intent intent = getIntent();
        if(intent!=null && intent.hasExtra("total")){
            resultado = intent.getDoubleExtra("total",0);
            textViewTotal.setText(resultado.toString());
        }
    }//
}//class

- CafeActivity.java
[source,java]
import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.TextView;
import androidx.activity.EdgeToEdge;
import androidx.appcompat.app.AppCompatActivity;
import androidx.core.graphics.Insets;
import androidx.core.view.ViewCompat;
import androidx.core.view.WindowInsetsCompat;
public class CafeActivity extends AppCompatActivity {
    private EditText editTextQuantidade;
    private TextView textViewCafe;
    private Button buttonAdd;
    private Controller controller;
    private Double resultado = 0.0;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        EdgeToEdge.enable(this);
        setContentView(R.layout.activity_cafe);
        controller = new Controller();
        Intent intent = getIntent();
        if(intent != null && intent.hasExtra("total")){
            resultado = intent.getDoubleExtra("total",0);
        }
        editTextQuantidade = findViewById(R.id.editTextCafe);
        textViewCafe = findViewById(R.id.textViewCafe);
        buttonAdd = findViewById(R.id.buttonCafe);
        buttonAdd.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                String texto = editTextQuantidade.getText().toString();
                Double parcial = controller.adicionar(getResources().getString(R.string.cafe),texto);
                resultado = resultado + parcial;
                textViewCafe.setText(parcial.toString());
            }
        });
        textViewCafe.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Intent intent = new Intent(CafeActivity.this, MainActivity.class);
                intent.putExtra("total",resultado);
                startActivity(intent);
            }
        });
    }
}

- BoloActivity.java
[source,java]
import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.TextView;
import androidx.activity.EdgeToEdge;
import androidx.appcompat.app.AppCompatActivity;
import androidx.core.graphics.Insets;
import androidx.core.view.ViewCompat;
import androidx.core.view.WindowInsetsCompat;
public class BoloActivity extends AppCompatActivity {
    private EditText editTextQuantidade;
    private TextView textViewBolo;
    private Button buttonAdd;
    private Controller controller;
    private Double resultado = 0.0;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        EdgeToEdge.enable(this);
        setContentView(R.layout.activity_bolo);
        controller = new Controller();
        //recebe o total do BoloActivity
        Intent intent = getIntent();
        if(intent != null && intent.hasExtra("total")){
            resultado = intent.getDoubleExtra("total",0);
        }
        editTextQuantidade = findViewById(R.id.editTextBolo);
        textViewBolo = findViewById(R.id.textViewBolo);
        buttonAdd = findViewById(R.id.buttonBolo);
        buttonAdd.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                String texto = editTextQuantidade.getText().toString();
                Double parcial = controller.adicionar(getResources().getString(R.string.bolo),texto);
                resultado = resultado + parcial;
                textViewBolo.setText(parcial.toString());
            }
        });
        textViewBolo.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Intent intent = new Intent(BoloActivity.this, MainActivity.class);
                intent.putExtra("total",resultado);
                startActivity(intent);
            }
        });
    }
}

- Calculo.java
[source,java]
public class Calculo {
    private static final double PRECO_CAFE = 2.50;
    private static final double PRECO_BOLO = 5.00;
    public Calculo() {
    }
    public double adicionarCafe(int qtd) {
        if (qtd > 0) {
            return qtd * PRECO_CAFE;
        }
        return 0;
    }
    public double adicionarBolo(int qtd) {
        if (qtd > 0) {
            return qtd * PRECO_BOLO;
        }
        return 0;
    }
}//class

- Controller.java
[source,java]
public class Controller {
    private  Calculo model;
    public Controller() {
        this.model = new Calculo();
    }
    public Double adicionar(String texto, String textoQuantidade) {
        int qtd = Integer.parseInt(textoQuantidade);
        if(texto.equals("Café")){
            return model.adicionarCafe(qtd);
        }
        if(texto.equals("Bolo")){
            return model.adicionarBolo(qtd);
        }
        return 0.0;
    }
}//class

- MainActivity.kt
[source,kotlin]
import android.content.Intent
import android.os.Bundle
import android.widget.ImageView
import android.widget.TextView
import androidx.activity.enableEdgeToEdge
import androidx.appcompat.app.AppCompatActivity
import androidx.core.view.ViewCompat
import androidx.core.view.WindowInsetsCompat
class MainActivity : AppCompatActivity() {
    private lateinit var imageViewCafe: ImageView
    private lateinit var imageViewBolo: ImageView
    private lateinit var textViewTotal: TextView
    private var resultado: Double = 0.0
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        setContentView(R.layout.activity_main)
        imageViewCafe = findViewById(R.id.imageViewCafe)
        imageViewBolo = findViewById(R.id.imageViewBolo)
        textViewTotal = findViewById(R.id.textViewTotal)
        imageViewBolo.setOnClickListener {
            val intent = Intent(this, BoloActivity::class.java).apply {
                putExtra("total", resultado)
            }
            startActivity(intent)
        }
        imageViewCafe.setOnClickListener {
            val intent = Intent(this, CafeActivity::class.java).apply {
                putExtra("total", resultado)
            }
            startActivity(intent)
        }
    }
    override fun onResume() {
        super.onResume()
        atualizarTela()
    }
    private fun atualizarTela() {
        if (intent.hasExtra("total")) {
            resultado = intent.getDoubleExtra("total", 0.0)
            textViewTotal.text = resultado.toString()
        }
    }
}

- CafeActivity.kt
[source,kotlin]
import android.content.Intent
import android.os.Bundle
import android.widget.Button
import android.widget.EditText
import android.widget.TextView
import androidx.activity.enableEdgeToEdge
import androidx.appcompat.app.AppCompatActivity
class CafeActivity : AppCompatActivity() {
    private lateinit var editTextQuantidade: EditText
    private lateinit var textViewCafe: TextView
    private lateinit var buttonAdd: Button
    private lateinit var controller: Controller
    private var resultado: Double = 0.0
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        setContentView(R.layout.activity_cafe)
        controller = Controller()
        if (intent.hasExtra("total")) {
            resultado = intent.getDoubleExtra("total", 0.0)
        }
        editTextQuantidade = findViewById(R.id.editTextCafe)
        textViewCafe = findViewById(R.id.textViewCafe)
        buttonAdd = findViewById(R.id.buttonCafe)
        buttonAdd.setOnClickListener {
            val texto = editTextQuantidade.text.toString()
            val parcial = controller.adicionar(getString(R.string.cafe), texto)
            resultado += parcial 
            textViewCafe.text = parcial.toString()
        }
        textViewCafe.setOnClickListener {
            val intent = Intent(this, MainActivity::class.java).apply {
                putExtra("total", resultado)
            }
            startActivity(intent)
        }
    }
}

- BoloActivity.kt
[source,kotlin]
import android.content.Intent
import android.os.Bundle
import android.widget.Button
import android.widget.EditText
import android.widget.TextView
import androidx.activity.enableEdgeToEdge
import androidx.appcompat.app.AppCompatActivity
class BoloActivity : AppCompatActivity() {
    private lateinit var editTextQuantidade: EditText
    private lateinit var textViewBolo: TextView
    private lateinit var buttonAdd: Button
    private lateinit var controller: Controller
    private var resultado: Double = 0.0
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        setContentView(R.layout.activity_bolo)
        controller = Controller()
        if (intent.hasExtra("total")) {
            resultado = intent.getDoubleExtra("total", 0.0)
        }
        editTextQuantidade = findViewById(R.id.editTextBolo)
        textViewBolo = findViewById(R.id.textViewBolo)
        buttonAdd = findViewById(R.id.buttonBolo)
        buttonAdd.setOnClickListener {
            val texto = editTextQuantidade.text.toString()
            val parcial = controller.adicionar(getString(R.string.bolo), texto)
            resultado += parcial
            textViewBolo.text = parcial.toString()
        }
        textViewBolo.setOnClickListener {
            val intent = Intent(this, MainActivity::class.java).apply {
                putExtra("total", resultado)
            }
            startActivity(intent)
        }
    }
}

- Calculo.kt
[source,kotlin]
class Calculo {
    companion object {
        private const val PRECO_CAFE = 2.50
        private const val PRECO_BOLO = 5.00
    }
    fun adicionarCafe(qtd: Int): Double {
        return if (qtd > 0) qtd * PRECO_CAFE else 0.0
    }
    fun adicionarBolo(qtd: Int): Double {
        return if (qtd > 0) qtd * PRECO_BOLO else 0.0
    }
}

- Controller.kt
[source,kotlin]
class Controller {
    private val model: Calculo = Calculo()
    fun adicionar(texto: String, textoQuantidade: String): Double {
        val qtd = textoQuantidade.toIntOrNull() ?: 0
        return when (texto) {
            "Café" -> model.adicionarCafe(qtd)
            "Bolo" -> model.adicionarBolo(qtd)
            else -> 0.0
        }
    }
}
